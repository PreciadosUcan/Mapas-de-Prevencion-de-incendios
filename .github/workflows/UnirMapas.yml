name: Ejecutar

on:  
  schedule:
    - cron: '0 6 * * *'
jobs: 
  build: 
    runs-on: windows-latest

    steps: 
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.14.3' 
      - run: python -/Roadmap/Unificar SHP  de incendios.py
